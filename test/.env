#DATABASE

USER="dev"
PORT="5432"
HOST="localhost"
DBNAME="bank"
PASSWORD="root"
DATABASE="postgres"
DBSTRING="user=${USER} password=${PASSWORD} dbname=${DBNAME} host=${HOST} port=${PORT} sslmode=disable"
MIGRATION_DIR=../../../../infra/db/migrations

#GOOSE

GOOSE_DRIVER=${DATABASE}
GOOSE_DBSTRING=${DBSTRING}
GOOSE_MIGRATION_DIR=${MIGRATION_DIR}
